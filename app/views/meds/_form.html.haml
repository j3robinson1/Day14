= form_for [@hospital, @patient, @med] do |f|
  = f.label :name
  = f.text_field :name
  %br/
  = f.label :direction
  = f.text_field :direction
  %br/
  = f.submit
  |
  \#{link_to "Back", hospital_patient_meds_path}