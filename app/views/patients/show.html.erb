<%= link_to 'Back', hospital_patients_path %>|
<%= link_to 'Edit', edit_hospital_patient_path(@hospital, @patient) %>
<h4>firstname: <%= @patient.firstname %></h4>
<h4>lastname: <%= @patient.lastname %></h4>
<div>Meds</div>
<% @meds.each do |med| %>
      <div><%= med.name %> <%= med.direction %>
        <%= link_to 'Show', hospital_patient_med_path(@hospital, @patient, med) %>

<% end %>
<div>
  <%= link_to "Add Med", new_hospital_patient_med_path(@hospital, @patient) %></div>

